<div>
  <h1 class="font-bold text-4xl">Stores</h1>
  <ul class="text-xl mt-4 list-disc list-inside">
  <% for route in @config["routes"] %>
  <% next unless route["endpoint"] %>
  <li class="my-2"><%= link_to route["name"], route["endpoint"] %></li>
  <% end %>
  </ul>
</div>
